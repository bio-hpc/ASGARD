
NOTE: leap spills out a bit more warning information if
ipol set by the user is different than the one read from frcmod

Also
3) leap refuses to saveamberparm if ipol>0, or saveamberparmpol if ipol = 0.

