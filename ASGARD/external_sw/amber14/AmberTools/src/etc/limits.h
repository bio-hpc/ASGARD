#ifdef CYGWIN
      parameter (maxq   = 500)
      parameter (maxlgr = 100)
      parameter (maxmol = 30)
#else
      parameter (maxq   = 8000)
      parameter (maxlgr = 800)
      parameter (maxmol = 300)
#endif
